# Real-ISO test cases — exercises the pipeline with actual game archives.
# These archives live in test/fixtures/isos/ and are gitignored (too large
# for version control). Run test/fixtures/create_fixtures.sh first to
# generate the synthetic fixtures; place real archives here manually.
#
# This file is used by Test 21 in test/run_tests.sh, which skips gracefully
# when the archive is absent so the main test suite still passes everywhere.
#
# format: ~iso_path|adapter|destination~

~/mnt/c/Github/loadout-pipeline/test/fixtures/isos/Ultimate Board Game Collection (USA).7z|sd|ps2~
